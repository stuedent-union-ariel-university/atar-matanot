# Copy this file to .env.local (never commit .env.local)
# Provide the real IDs and API key values.

# Monday API personal token with access to the relevant boards
MONDAY_API_KEY=your_monday_api_token_here

# Board that contains the list of eligible students (numeric ID from Monday URL)
MONDAY_USER_BOARD_ID=1234567890

# Board that will store each gift claim (one item per claim)
MONDAY_CLAIMS_BOARD_ID=1234567891

# Column ID in the eligibility board that stores the userId (e.g. text / text1 / some custom id)
MONDAY_USER_BOARD_USER_ID_COLUMN_ID=text

# Column ID in the claims board for the userId (defaults to 'text' if not set at runtime)
MONDAY_CLAIMS_BOARD_USER_ID_COLUMN_ID=text

# Column ID in the claims board for the gift title (defaults to 'text1' if not set at runtime)
MONDAY_CLAIMS_BOARD_GIFT_TITLE_COLUMN_ID=text1

# Inventory board (for stock management) — required for upload script and live inventory
# Board where each item is a gift. It must have:
#  - A Text column to hold the giftId from src/lib/gifts.ts (e.g., "eir-gps-tag")
#  - A Numbers column to hold the stock value
MONDAY_INVENTORY_BOARD_ID=1234567892
MONDAY_INVENTORY_GIFT_ID_COLUMN_ID=text
MONDAY_INVENTORY_STOCK_COLUMN_ID=numbers
